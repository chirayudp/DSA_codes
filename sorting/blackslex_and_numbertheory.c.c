#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main(){
long long t;
scanf("%lld",&t);
for (int i = 0; i < t; i++)
{
    long long n;
    scanf("%lld",&n);
    for (int i = 0; i < n; i++)
    {
        long long n;
        scanf("%lld",&n);
    }
    
}

}
