#include<bits/stdc++.h>
using namespace std;

bool isbridge(vector<vector<int>>&adj){
    int v= adj.size();
    vector<int> vis(v,0);
    vector<int> tin(v),low(v);//time of insertion of node

}

int main(){
int n; cin >> n;
vector<vector<int>> adj = {{2,3},{1,3},{2,4},{5,6},{4,6},{4,5}};

}